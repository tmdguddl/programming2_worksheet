#include <stdio.h>

int main() {
    int n;
    printf("자연수 입력: ");
    scanf("%d", &n);
    
    int value = 1;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= 2 * i - 1; j++) {
            printf("%d ", value++);
        }
        printf("\n");
    }
}
