#include <stdio.h>

int main() {
    int grade, student_id;
    char name[30];
    printf("학년: ");
    scanf("%d", &grade);
    printf("학번: ");
    scanf("%d", &student_id);
    printf("이름: ");
    scanf("%s", name);

    printf("%d학년 %d %s\n", grade, student_id, name);
}
