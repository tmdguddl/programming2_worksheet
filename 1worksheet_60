#include <stdio.h>

int main()
{
   int x;

   scanf_s("%d", &x);
   printf("x=%d\n", x);
   printf("%d\n", x & (1 << 2));
   printf("%d\n", x & (1 << 2));

   printf("%d의 아래 4자리 \n", x & 15);

   for (int i = 1; i <= 4; i++)
      printf("%d번 시프트: %d\n", i, x<<i);
}
